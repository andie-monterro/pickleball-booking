# Postgres connection string. Production points at Neon; tests provision their
# own throwaway container and ignore this value.
DATABASE_URL=

# API key used by the production phone verification adapter.
PRELUDE_API_KEY=
